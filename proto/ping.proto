syntax = "proto3";

package file;

option go_package = "./pb";

message PingRequest {};
message PingResponse {
  string message = 1;
};

service PingService {
  rpc Ping(PingRequest) returns (PingResponse);
}
